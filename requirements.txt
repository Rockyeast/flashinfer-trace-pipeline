# Local pipeline dependencies.
#
# CUDA/SGLang/FlashInfer runtime packages are installed inside the Modal images
# used by probe, collect, and reference-test jobs.
pydantic>=2,<3
modal>=1
huggingface-hub>=0.34,<1.0
PyYAML>=6
safetensors>=0.4
flashinfer-bench

# Optional: required only for parse diagnostics with --llm-classify.
anthropic>=0.50
